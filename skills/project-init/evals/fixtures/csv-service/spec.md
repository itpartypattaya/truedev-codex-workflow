# CSV normalization service

Build a backend-only Python service that accepts CSV uploads, validates required columns, normalizes
valid rows, and returns a machine-readable error report. There is no browser UI. Do not retain input
files after processing. The delivery plan must identify automated acceptance evidence.
